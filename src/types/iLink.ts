import { _iIcon, iIcon } from "./iIcon";

export interface iLink {
    icon: iIcon,
    link: string,
    name: string,
}

export interface _iLink {
    icon: _iIcon,
    link: string,
    name: string,
}